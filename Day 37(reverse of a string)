#include <iostream>
#include <string>
using namespace std;

int main(){
    string str;
    cout<<"Enter text: ";
    getline(cin,str);

    string reverse = string(str.rbegin() , str.rend());
    cout<<"reverse of a text is: "<<reverse;

    return 0;
}
